package com.example.chatmobile

import androidx.compose.foundation.layout.PaddingValues
import androidx.compose.runtime.Composable
import androidx.compose.runtime.getValue
import androidx.compose.runtime.mutableStateOf
import androidx.compose.runtime.remember
import androidx.compose.runtime.setValue
import androidx.lifecycle.viewmodel.compose.viewModel

import com.example.chatmobile.screen.UsernameScreen

@Composable
fun ChatApp() {/*
   val viewModel:ChatViewModel=viewModel{ChatViewModel()}
   var username by remember { mutableStateOf<String?>(null) }

   if (username == null) {
      UsernameScreen { enteredUsername ->
         username = enteredUsername
         viewModel.connect(enteredUsername)
      }
   } else {
      ChatScreen(username = username!!, viewModel = viewModel)
   }*/
}
